<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.HireMapper">
    <!-- insertHire -->
  <!--   <insert id="insertHire" parameterType="Hire">
    insert into tb_hire(company_id,person_id,post_id,status) values(#{company_id},#{person_id},#{post_id},#{status})
    </insert> -->
</mapper>